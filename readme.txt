# Todo App React

```bash
npm init
npm install parcel-bundler --save-dev
npm run start
```

Dont forge to add the `start` script to the `package.json` file.

```json
"scripts": {
    "start": "parcel index.html"
}
```
